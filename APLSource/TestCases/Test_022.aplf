 R←Test_022(stopFlag batchFlag);rp;⎕TRAP
⍝ Exercise `OpenAndCreateKey`
 R←T._Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 rp←GetRegPath

⍝ Tests
 :Trap 11
     →T.FailsIf 0≠##.WinReg.OpenAndCreateKey'HKEY_ThisMustFail\Fully'
     →0
 :Else
     →T.FailsIf'WinReg error: invalid Keyword'≢0⊃⎕DM
 :EndTrap

 :Trap 11
     →T.FailsIf 0≠##.WinReg.OpenAndCreateKey⍕⍳88888
     →0
 :Else
     →T.FailsIf'WinReg error: opening/creating Registry key failed with RC=206'≢0⊃⎕DM
 :EndTrap

 R←T._OK
