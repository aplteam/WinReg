 R←Test_027(stopFlag batchFlag);name;val;all;⎕TRAP
⍝ Exercise `GetErrorAsStringFrom`
 R←∆Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

⍝ Tests
 :For name val :InEach ↓⍉##.WinReg.ListError
     →FailsIf name≢##.WinReg.GetErrorAsStringFrom val
 :EndFor

 all←##.WinReg.ListError
 →FailsIf all≢##.WinReg.GetErrorAsStringFrom''

 R←∆OK
