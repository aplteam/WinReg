 R←Test_023(stopFlag batchFlag);path;⎕TRAP
⍝ Exercise error messages with `GetValue`
 R←T._Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 path←GetRegPath

⍝ Preconditions
 ##.WinReg.DeleteSubKeyTree path                         ⍝ Delete any left-overs

⍝ Tests..,
   ⍝ Write string "%WinDir%" and read it back (must remain unchanged!)
 ##.WinReg.PutString(path,'str1_')'AnyString'

 :Trap 11
     ##.WinReg.GetValue 3⍴⊂path,'str1_'
     →0
 :Else
     →T.FailsIf'WinReg error: invalid right argument'≢0⊃⎕DM
 :EndTrap

 :Trap 11
     ##.WinReg.GetValue''
     →0
 :Else
     →T.FailsIf'WinReg error: right argument must not be empty'≢0⊃⎕DM
 :EndTrap

 :Trap 11
     ##.WinReg.GetValue 123
     →0
 :Else
     →T.FailsIf'WinReg error: invalid right argument'≢0⊃⎕DM
 :EndTrap

⍝ Tidy up
 ##.WinReg.DeleteSubKeyTree path

 R←T._OK
